# Generated by Skyramp v1.3.14 on 2026-03-20 17:29:52.466676709 +0000 UTC m=+181.474596161
# Command: skyramp generate integration rest \
# 		--auth-header Authorization \
# 		--framework pytest \
# 		--language python \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/prefect-testb/prefect-testb/tests/scenario_flow-quota-set-and-retrieve.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = os.getenv("SKYRAMP_TEST_TOKEN")

    # Request Body
    limits_POST_request_body = r'''{}'''
    
    # Execute Request
    limits_POST_response = client.send_request(
        url=URL_localhost,
        path="/api/flow-quotas/limits",
        method="POST",
        body=limits_POST_request_body,
        headers=headers,
        query_params={
            "flow_id": ["test-flow-quota-1"],
            "max_concurrent_runs": ["10"],
            "max_daily_runs": ["100"]
        }
    )
    # Generated Assertions
    assert limits_POST_response.status_code == 201

    # Execute Request
    limits_GET_response = client.send_request(
        url=URL_localhost,
        path="/api/flow-quotas/limits",
        method="GET",
        headers=headers
    )
    # Generated Assertions
    assert limits_GET_response.status_code == 200


if __name__ == "__main__":
    test_integration()

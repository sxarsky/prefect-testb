# Generated by Skyramp v1.3.14 on 2026-03-20 17:38:19.181066701 +0000 UTC m=+132.217510471
# Command: skyramp generate integration rest \
# 		--auth-header Authorization \
# 		--framework pytest \
# 		--language python \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/prefect-testb/prefect-testb/tests/scenario_task_validation_workflow.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = os.getenv("SKYRAMP_TEST_TOKEN")

    # Declaration of variables
    task_name = "process_data"

    # Request Body
    endpoint_1_POST_request_body = r'''{
        "properties": {
            "result": {
                "type": "number"
            },
            "status": {
                "type": "string"
            }
        },
        "type": "object"
    }'''
    
    # Execute Request
    endpoint_1_POST_response = client.send_request(
        url=URL_localhost,
        path="/api/task-validation-v2/schemas/",
        method="POST",
        body=endpoint_1_POST_request_body,
        headers=headers,
        query_params={
            "schema_type": ["pydantic"],
            "task_name": task_name
        }
    )
    # Generated Assertions
    assert endpoint_1_POST_response.status_code == 200

    # Request Body
    endpoint_2_POST_request_body = r'''{
        "result": 42,
        "status": "success"
    }'''
    
    # Execute Request
    endpoint_2_POST_response = client.send_request(
        url=URL_localhost,
        path="/api/task-validation-v2/validate/",
        method="POST",
        body=endpoint_2_POST_request_body,
        headers=headers,
        query_params={
            "schema_id": [skyramp.get_response_value(endpoint_1_POST_response, "id")],
            "task_name": skyramp.get_response_value(endpoint_1_POST_response, "task_name")
        }
    )
    # Generated Assertions
    assert endpoint_2_POST_response.status_code == 200


if __name__ == "__main__":
    test_integration()

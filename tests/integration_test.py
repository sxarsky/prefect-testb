# Generated by Skyramp v1.3.14 on 2026-03-20 18:38:21.251000823 +0000 UTC m=+138.093086059
# Command: skyramp generate integration rest \
# 		--framework pytest \
# 		--language python \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/prefect-testb/prefect-testb/tests/scenario_retry-budget-workflow.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Declaration of variables
    runs = "3fa85f64-5717-4562-b3fc-2c963f66afa6"

    # Request Body
    budgets_POST_request_body = r'''{}'''
    
    # Execute Request
    budgets_POST_response = client.send_request(
        url=URL_localhost,
        path="/task-retries/budgets",
        method="POST",
        body=budgets_POST_request_body,
        query_params={
            "flow_id": ["flow-test-abc"],
            "max_retries": ["10"]
        }
    )
    # Generated Assertions
    assert budgets_POST_response.status_code == 200

    # Execute Request
    budgets_GET_response = client.send_request(
        url=URL_localhost,
        path="/task-retries/budgets",
        method="GET"
    )
    # Generated Assertions
    assert budgets_GET_response.status_code == 200

    # Execute Request
    usage_GET_response = client.send_request(
        url=URL_localhost,
        path="/task-retries/runs/{runs}/usage",
        method="GET",
        path_params={"runs": runs}
    )
    # Generated Assertions
    assert usage_GET_response.status_code == 200


if __name__ == "__main__":
    test_integration()

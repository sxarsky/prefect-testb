# Generated by Skyramp v1.3.14 on 2026-03-20 18:31:27.597041741 +0000 UTC m=+125.628176713
# Command: skyramp generate integration rest \
# 		--auth-header Authorization \
# 		--chaining-key flow_id \
# 		--framework pytest \
# 		--language python \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/prefect-testb/prefect-testb/tests/scenario_set_quota_then_check_usage.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = os.getenv("SKYRAMP_TEST_TOKEN")

    # Declaration of variables
    flow_id = "flow-1"

    # Request Body
    limits_POST_request_body = r'''{
        "flow_id": "flow-1",
        "max_concurrent_runs": 10,
        "max_daily_runs": 100
    }'''
    
    # Execute Request
    limits_POST_response = client.send_request(
        url=URL_localhost,
        path="/flow-quotas/limits",
        method="POST",
        body=limits_POST_request_body,
        headers=headers,
        data_override={"flow_id": flow_id}
    )
    # Generated Assertions
    assert limits_POST_response.status_code == 200

    # Execute Request
    usage_GET_response = client.send_request(
        url=URL_localhost,
        path="/flow-quotas/usage/{usage}",
        method="GET",
        headers=headers,
        path_params={"usage": flow_id}
    )
    # Generated Assertions
    assert usage_GET_response.status_code == 200


if __name__ == "__main__":
    test_integration()

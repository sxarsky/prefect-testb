# Generated by Skyramp v1.3.14 on 2026-03-22 20:13:24.403261781 +0000 UTC m=+155.778913578
# Command: skyramp generate integration rest \
# 		--auth-header Authorization \
# 		--chaining-key id \
# 		--framework pytest \
# 		--language python \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/prefect-testb/prefect-testb/tests/scenario_checkpoint_create_and_retrieve_workflow.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = os.getenv("SKYRAMP_TEST_TOKEN")

    # Declaration of variables
    flow_runs = "3fa85f64-5717-4562-b3fc-2c963f66afa6"

    # Request Body
    endpoint_1_POST_request_body = r'''{
        "metadata": {
            "data_size_mb": 125,
            "task_count": 1
        },
        "name": "data_extracted"
    }'''
    
    # Execute Request
    endpoint_1_POST_response = client.send_request(
        url=URL_localhost,
        path="/checkpoints-v2/flow-runs/{flow-runs}/checkpoints/",
        method="POST",
        body=endpoint_1_POST_request_body,
        headers=headers,
        path_params={"flow-runs": flow_runs}
    )
    # Generated Assertions
    assert endpoint_1_POST_response.status_code == 200

    # Execute Request
    checkpoints_GET_response = client.send_request(
        url=URL_localhost,
        path="/checkpoints-v2/checkpoints/{checkpoints}",
        method="GET",
        headers=headers,
        path_params={"checkpoints": skyramp.get_response_value(endpoint_1_POST_response, "id")}
    )
    # Generated Assertions
    assert checkpoints_GET_response.status_code == 200


if __name__ == "__main__":
    test_integration()

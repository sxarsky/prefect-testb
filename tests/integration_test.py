# Generated by Skyramp v1.3.14 on 2026-03-20 17:29:58.549892689 +0000 UTC m=+131.972241407
# Command: skyramp generate integration rest \
# 		--auth-header Authorization \
# 		--chaining-key checkpoint_id \
# 		--framework pytest \
# 		--language python \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/prefect-testb/prefect-testb/tests/scenario_flow_checkpoint_workflow.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = os.getenv("SKYRAMP_TEST_TOKEN")

    # Declaration of variables
    runs = "3fa85f64-5717-4562-b3fc-2c963f66afa6"

    # Request Body
    runs_POST_request_body = r'''{
        "state_data": {
            "data": "checkpoint-data",
            "step": 1
        },
        "task_name": "task-1"
    }'''
    
    # Execute Request
    runs_POST_response = client.send_request(
        url=URL_localhost,
        path="/flow-checkpoints/runs/{runs}",
        method="POST",
        body=runs_POST_request_body,
        headers=headers,
        path_params={"runs": runs}
    )
    # Generated Assertions
    assert runs_POST_response.status_code == 200

    # Execute Request
    runs_GET_response = client.send_request(
        url=URL_localhost,
        path="/flow-checkpoints/runs/{runs}",
        method="GET",
        headers=headers,
        path_params={"runs": skyramp.get_response_value(runs_POST_response, "run_id")}
    )
    # Generated Assertions
    assert runs_GET_response.status_code == 200

    # Request Body
    restore_POST_request_body = r'''{
        "checkpoint_id": "new-checkpoint"
    }'''
    
    # Execute Request
    restore_POST_response = client.send_request(
        url=URL_localhost,
        path="/flow-checkpoints/runs/{runs}/restore",
        method="POST",
        body=restore_POST_request_body,
        headers=headers,
        path_params={"runs": skyramp.get_response_value(runs_POST_response, "run_id")},
        data_override={"checkpoint_id": skyramp.get_response_value(runs_POST_response, "checkpoint_id")}
    )
    # Generated Assertions
    assert restore_POST_response.status_code == 200


if __name__ == "__main__":
    test_integration()

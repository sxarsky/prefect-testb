# Generated by Skyramp v1.3.14 on 2026-03-22 20:29:05.733821126 +0000 UTC m=+129.574056258
# Command: skyramp generate contract rest http://localhost:4200/api/retry-budgets-v2/retry-budget-overview/ \
# 		--auth-header Authorization \
# 		--framework pytest \
# 		--language python \
# 		--method GET \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--provider \
# 		--runtime local

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

# contract test for /api/retry-budgets-v2/retry-budget-overview/ GET
def test_retry_budget_overview_get():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = os.getenv("SKYRAMP_TEST_TOKEN")

    # Execute Request
    retry_budget_overview_GET_response = client.send_request(
        url=URL,
        path="/api/retry-budgets-v2/retry-budget-overview/",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert skyramp.check_status_code(retry_budget_overview_GET_response, "20x")


if __name__ == "__main__":
    test_retry_budget_overview_get()

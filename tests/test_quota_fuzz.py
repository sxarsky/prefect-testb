# Generated by Skyramp v1.3.13 on 2026-03-16 21:15:29.789551785 +0000 UTC m=+194.502696514
# Command: skyramp generate fuzz rest http://localhost:4200/api/quotas/flows/11111111-1111-1111-1111-111111111111/quota/ \
# 		--framework pytest \
# 		--language python \
# 		--method POST \
# 		--output test_quota_fuzz.py \
# 		--output-dir /home/runner/work/prefect-testb/prefect-testb/tests \
# 		--overwrite \
# 		--provider \
# 		--request-data {"flow_id":"11111111-1111-1111-1111-111111111111","max_concurrent_runs":5,"max_runs_per_hour":50,"max_runs_per_day":200,"quota_exceeded_action":"reject"} \
# 		--runtime local \
# 		--skipProvisionParents

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:4200")

# fuzz test for /api/quotas/flows/11111111-1111-1111-1111-111111111111/quota/ POST
def test_quota_post():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Request Body
    quota_POST_request_body = r'''{
      "flow_id": "11111111-1111-1111-1111-111111111111",
      "max_concurrent_runs": 5,
      "max_runs_per_hour": 50,
      "max_runs_per_day": 200,
      "quota_exceeded_action": "reject"
    }'''
    
    # Fuzz strategies
    quota_post_fuzzed_body = {
        "flow_id": "0123456789",
        "max_concurrent_runs": -10,
        "max_runs_per_day": -10,
        "max_runs_per_hour": -10,
        "quota_exceeded_action": "0123456789"
    }
    # Fuzz status codes
    expected_quota_post_status_code = {
        "flow_id": "40x",
        "max_concurrent_runs": "40x",
        "max_runs_per_day": "40x",
        "max_runs_per_hour": "40x",
        "quota_exceeded_action": "40x"
    }
    # Fuzz status codes for Null values
    expected_quota_post_null_status_code = {
        "flow_id": "40x",
        "max_concurrent_runs": "40x",
        "max_runs_per_day": "40x",
        "max_runs_per_hour": "40x",
        "quota_exceeded_action": "40x"
    }

    # Execute Request
    quota_POST_response = client.send_request(
        url=URL,
        path="/api/quotas/flows/11111111-1111-1111-1111-111111111111/quota/",
        method="POST",
        body=quota_POST_request_body,
        headers=headers,
        expected_code="20x"
    )

    for key in skyramp.iterate(quota_post_fuzzed_body):
        # Execute Request
        quota_POST_response = client.send_request(
            url=URL,
            path="/api/quotas/flows/11111111-1111-1111-1111-111111111111/quota/",
            method="POST",
            body=quota_POST_request_body,
            headers=headers,
            data_override={key: skyramp.get_value(quota_post_fuzzed_body, key)},
            expected_code=skyramp.get_value(expected_quota_post_status_code, key),
            description=f'Fuzzing request body { key } to { skyramp.get_value(quota_post_fuzzed_body, key) }'
        )

        # Execute Request
        quota_POST_response = client.send_request(
            url=URL,
            path="/api/quotas/flows/11111111-1111-1111-1111-111111111111/quota/",
            method="POST",
            body=quota_POST_request_body,
            headers=headers,
            data_override={key: None},
            expected_code=skyramp.get_value(expected_quota_post_null_status_code, key),
            description=f'Fuzzing request body { key } to None'
        )

    assert client.is_success()


if __name__ == "__main__":
    test_quota_post()
